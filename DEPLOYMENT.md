# 🚀 Инструкция по деплою на GitHub Pages

## Подготовка к деплою

### 1. Настройка имени репозитория

Если ваш репозиторий называется не `NutriCalc`, нужно изменить base path в `vite.config.ts`:

```typescript
// vite.config.ts
base: process.env.NODE_ENV === 'production' ? '/ваше-имя-репозитория/' : '/',
```

**Важно:** Имя репозитория должно совпадать с base path в конфигурации!

### 2. Активация GitHub Pages

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе `Source` выберите:
   - **Source**: `GitHub Actions`
3. Сохраните изменения

### 3. Настройка прав доступа

GitHub Actions автоматически получит необходимые права после первого запуска workflow.

## Автоматический деплой

### Через GitHub Actions (рекомендуется)

1. **Push в main ветку:**
   ```bash
   git add .
   git commit -m "Deploy to GitHub Pages"
   git push origin main
   ```

2. **Проверка деплоя:**
   - Перейдите в раздел `Actions` в вашем репозитории
   - Дождитесь завершения workflow `Deploy to GitHub Pages`
   - При успешном деплое приложение будет доступно по адресу:
     `https://[ваш-username].github.io/NutriCalc/`

3. **Ручной запуск:**
   - Перейдите в `Actions` → `Deploy to GitHub Pages`
   - Нажмите `Run workflow`

### Через npm скрипт (альтернативный способ)

```bash
# Установите gh-pages если еще не установлен
npm install --save-dev gh-pages

# Деплой
npm run deploy
```

Это создаст ветку `gh-pages` с собранным приложением.

## Проверка деплоя

После успешного деплоя проверьте:

1. ✅ Приложение открывается по правильному URL
2. ✅ Все роуты работают (/, /fertilizers, /settings, /watering, /recipe)
3. ✅ PWA функциональность работает
4. ✅ Темы переключаются корректно
5. ✅ Локализация работает

## Решение проблем

### Проблема: 404 при переходе на страницы

**Решение:** Убедитесь, что:
- Base path в `vite.config.ts` совпадает с именем репозитория
- `Router` в `App.tsx` использует `basename={import.meta.env.BASE_URL}`

### Проблема: Статические файлы не загружаются

**Решение:** Проверьте, что `base` path в `vite.config.ts` заканчивается на `/`

### Проблема: GitHub Actions не запускается

**Решение:**
1. Проверьте, что workflow файл находится в `.github/workflows/deploy.yml`
2. Убедитесь, что в настройках репозитория включен `GitHub Actions`
3. Проверьте права доступа в `Settings` → `Actions` → `General`

## Обновление приложения

Для обновления приложения:

1. Внесите изменения в код
2. Commit и push в main ветку:
   ```bash
   git add .
   git commit -m "Update app"
   git push origin main
   ```
3. GitHub Actions автоматически пересоберет и задеплоит новую версию

## Дополнительные настройки

### Кастомный домен

Если вы хотите использовать кастомный домен:

1. Добавьте файл `CNAME` в папку `public/`:
   ```
   yourdomain.com
   ```

2. Настройте DNS записи у вашего провайдера домена

### Environment variables

Для использования переменных окружения в production:

1. Добавьте их в GitHub Secrets: `Settings` → `Secrets and variables` → `Actions`
2. Используйте в workflow файле:
   ```yaml
   env:
     MY_VAR: ${{ secrets.MY_SECRET }}
   ```

## Полезные ссылки

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html#github-pages)
- [React Router Basename](https://reactrouter.com/en/main/router-components/browser-router#basename)

